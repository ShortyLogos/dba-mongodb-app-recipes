commandes_git

# RÉCUPÉRER LE CONTENU DE LA BRANCHE DEV SUR MACHINE LOCALE
git init
git remote add origin https://github.com/manilboudjemai/orion.git (par exemple)
git branch -m dev  # préciser la branche depuis laquelle récupérer les dossiers et fichiers
git pull origin dev

# MERGER LES DERNIÈRES UPDATES DE LA BRANCHE DEV DANS VOTRE BRANCHE
git merge origin/dev    // à effectuer depuis votre branche

# MÉTHODOLOGIE POUR FAIRE UN PUSH :
1. git status   									// vérifie les fichiers staged/unstaged
2. git add .    									// stage tous les fichiers du répertoire (plus simple que de choisir fichier par fichier)
3. git commit -m "message significatif et court"    // message obligatorie sinon commit refusé
4. git push    										// push vers le répretoire github
5. vérification du pull request sur github. Si aucun conflit --> confirmer merge pull request
6. supprimer branche

# VÉRIFIER LES BRANCHES DE MON GIT LOCALE ET DANS LAQUELLE JE SUIS
git branch

# CHANGER DE BRANCHE
git checkout nom-de-la-branche

# CRÉER UNE NOUVELLE BRANCHE ET S'Y DIRIGER AUTOMATIQUEMENT
git checkout -b nom-de-la-branche
